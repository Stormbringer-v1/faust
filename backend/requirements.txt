# ──────────────────────────────────────────────────────────────────────
# Faust Backend — Python Dependencies
# ──────────────────────────────────────────────────────────────────────

# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9
email-validator>=2.1.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Cache / Message broker
redis>=5.0.0
celery>=5.3.0

# Auth / Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
slowapi>=0.1.9

# HTTP client (for AI providers, NVD API, EPSS, CISA KEV)
httpx>=0.27.0

# Async file operations
aiofiles>=23.2.1

# Scanning
python-nmap>=0.7.1

# AI providers (all optional — install based on DEFAULT_AI_PROVIDER)
anthropic>=0.43.0
openai>=1.60.0
google-genai>=1.0.0

# Report generation
jinja2>=3.1.0
weasyprint>=62.0

# Logging / Utilities
orjson>=3.9.0
